# Minimal dependencies for the preserved standalone demo server.
# The MVP's AgentScope boundary is implemented locally in agentscope_pipeline.py;
# installing the full agentscope package is optional and not required to run the entry page.
starlette>=0.48.0
uvicorn>=0.31.1
httpx>=0.28.0
anthropic>=0.40.0
socksio>=1.0.0
